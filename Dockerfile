# Используем базовый образ с установленной Java (предполагая, что ваш проект использует Java 17)
FROM openjdk:17-oracle
# Копируем JAR-файл из target директории в контейнер
COPY target/claim-api-1.0.jar /app.jar
# Устанавливаем рабочую директорию
WORKDIR /
# Открываем порт, на котором работает ваше приложение (замените 8080 на нужный вам порт)
EXPOSE 8080
# Команда для запуска приложения (может отличаться в зависимости от вашего приложения)
CMD ["java", "-jar", "/app.jar"]